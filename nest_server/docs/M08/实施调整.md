# M08 实施调整记录

## 2026-08-20

- 未实施方案第 17 节第 20 项“旧数据导入脚本”。原因：方案第 2.2 节将旧 MySQL 数据一次性导入明确列为 P1；本轮按用户指令仅实现 P0，避免提前引入旧库连接、导入格式与兼容逻辑。
- LLM 基础设施使用基于 Node 原生 `fetch` 的 OpenAI 兼容适配器，不新增 SDK。原因：当前工程没有可复用的 AI HTTP 客户端；原生 API 足以实现 P0 的超时、一次重试、响应 Schema 校验和脱敏错误映射。缺少完整 LLM 配置时仍明确返回 503，知识查询不受影响。
