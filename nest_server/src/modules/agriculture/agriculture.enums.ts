export enum CropStatus {
  Active = 'active',
  Inactive = 'inactive',
}
export enum KnowledgeStatus {
  Draft = 'draft',
  Published = 'published',
  Archived = 'archived',
}
export enum AnalysisStatus {
  Processing = 'processing',
  Succeeded = 'succeeded',
  Failed = 'failed',
}
